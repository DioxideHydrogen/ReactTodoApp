# Encaminhar todas as requisições para a pasta dist
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Evita loop quando a requisição já está em /dist
  RewriteCond %{REQUEST_URI} !^/dist/
  RewriteRule ^(.*)$ /dist/$1 [L]
</IfModule>